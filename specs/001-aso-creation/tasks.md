---
description: "Task list for ASO 创作 feature — generated by /speckit.tasks"
---

# Tasks: ASO 创作

**Input**: [spec.md](./spec.md) · [plan.md](./plan.md) · [data-model.md](./data-model.md) · [contracts/aso-api.md](./contracts/aso-api.md)

**Prerequisites**: plan.md ✅ · spec.md ✅ · clarify.md ✅ · research.md ✅

**Branch**: `001-aso-creation`

**Scope exclusions**: 多账号/用户隔离 **Out of Scope**（见 [clarify-multiuser.md](./clarify-multiuser.md)）

**Decisions applied** (2026-07-08): [clarify.md](./clarify.md) C-005 Vision · C-006 Toonflow-web fork · C-007 409 防重复 · C-008 素材=图/文

**Tests**: 本 feature spec **未要求**自动化测试；验收采用 `quickstart.md` 手动 curl + Phase 10 回归。

## Format: `[ID] [P?] [Story] Description`

- **[P]**: 可并行（不同文件、无未完成依赖）
- **[Story]**: 对应 spec 用户故事 `US1`–`US7`
- 路径默认：后端 `E:\workflow\toonflow\`（Toonflow-app）；前端 `E:\workflow\toonflow-web\`（Toonflow-web fork）

---

## Phase 0: Repo Setup（前端 fork）

**Purpose**: Clone Toonflow-web 到用户自有仓库，与后端并列开发

- [ ] T000 Clone `https://github.com/linqixin1003/Toonflow-web.git` → `E:\workflow\toonflow-web`，checkout/create branch `001-aso-creation`
- [ ] T000a Verify `yarn install && yarn dev` in Toonflow-web connects to Toonflow-app API (localhost:10588)
- [ ] T000b Document fork remote URL in Toonflow-web README (user's GitHub fork)

**Checkpoint**: Toonflow-web dev server runs; can open workbench against local backend

---

## Phase 1: Setup（共享基础设施）

**Purpose**: 常量、类型、Skill — 所有 User Story 的公共依赖

**⚠️ CRITICAL**: Phase 2 完成前不得开始 User Story 实现

- [ ] T001 [P] Create `src/constants/projectTypes.ts` with `ProjectTypeValue`, `PROJECT_TYPES`, `isAsoProject()`, `getProjectTypeConfig()`
- [ ] T002 [P] Create `src/constants/asoSizePresets.ts` with 12 presets from clarify.md, `getPresetById()`, `getDefaultPreset()`, `listPresetsGrouped()`
- [ ] T003 [P] Create `src/services/aso/types.ts` with Zod schemas: `AsoPlanSchema`, `AsoWorkspaceSchema`, `AsoOutputRecordSchema` and exported TS types
- [ ] T004 [P] Create `data/skills/aso_plan_generation.md` — frontmatter + prompt for N plans (title+copy), JSON array or XML output format
- [ ] T004a [P] Seed `o_agentDeploy` row `key='asoVisionAi'`, name「ASO 图片理解」in `src/lib/initDB.ts` (disabled until user configures model)
- [ ] T005 Run `yarn lint` in Toonflow-app root to verify Phase 1 compiles

**Checkpoint**: Constants importable; lint clean

---

## Phase 2: Foundational（阻塞性前置）

**Purpose**: workspace 服务层 + ASO 项目校验 — **所有 US 依赖此阶段**

**⚠️ CRITICAL**: User Story 工作不得在此阶段完成前开始

- [ ] T006 Create `src/services/aso/workspace.ts` with `fetchProject(projectId)` reading `o_project`
- [ ] T007 Implement `assertAsoProject(projectId)` in `src/services/aso/workspace.ts` — throws if `projectType !== 'aso'`
- [ ] T008 Implement `getDefaultWorkspace(): AsoWorkspace` in `src/services/aso/workspace.ts`
- [ ] T009 Implement `getOrCreateWorkspace(projectId)` — `o_agentWorkData` where `key='asoWorkspace'`, insert default JSON if missing
- [ ] T010 Implement `getWorkspace(projectId)` returning parsed workspace JSON
- [ ] T011 Implement `patchWorkspace(projectId, partial)` merge + persist with `updateTime`
- [ ] T012 Implement `setPlans()`, `updatePlanById()`, `appendOutput()`, `updateOutputState()` in `src/services/aso/workspace.ts`
- [ ] T013 Implement `syncReferencedAssets(projectId, assetId, 'add'|'remove')` in `src/services/aso/workspace.ts`
- [ ] T014 [P] Create `src/routes/aso/getSizePresets.ts` — GET presets list (no projectId), mount as `/api/aso/getSizePresets`
- [ ] T015 Restart `yarn dev` and verify `src/router.ts` auto-registers `/api/aso/*` routes

**Checkpoint**: `getOrCreateWorkspace` callable; `getSizePresets` returns 12 items

---

## Phase 3: User Story 1 — 创建 ASO 项目 (P1) 🎯

**Goal**: 新建项目可选「ASO 创作」，`projectType='aso'`，进入 ASO 工作区而非短剧流程

**Independent Test**: POST `/api/project/addProject` with `projectType: "aso"` → GET project list contains item → open workbench shows ASO UI only

### Implementation

- [ ] T016 [US1] Verify `src/routes/project/addProject.ts` accepts `projectType: "aso"` without code change (document in comment if needed)
- [ ] T017 [US1] [P] Add i18n key `workbench.project.dialog.basedOnAso` in **Toonflow-web** locales (zh-CN, zh-TW, en minimum)
- [ ] T018 [US1] Add `<t-option value="aso" :label="$t('workbench.project.dialog.basedOnAso')">` in **Toonflow-web** project create dialog
- [ ] T019 [US1] Hide or default-empty video fields (videoModel, videoRatio, directorManual) when `projectType==='aso'` in **Toonflow-web** create form
- [ ] T020 [US1] Add route guard in **Toonflow-web**: `projectType==='aso'` → `views/workbench/aso/AsoWorkbench.vue`
- [ ] T021 [US1] Create stub `AsoWorkbench.vue` in **Toonflow-web** with placeholder layout (proves routing works)

**Checkpoint**: Can create ASO project and land on empty ASO workbench; novel/script projects unchanged

---

## Phase 4: User Story 2 — 输入需求并流式生成创意方案 (P1)

**Goal**: 文本+图片输入 → SSE 流式生成 N 套方案（降级非流式）；持久化到 workspace

**Independent Test**: POST `/api/aso/generatePlans/stream` → receive `plan_delta` events → refresh `getWorkspace` shows N plans

### Implementation

- [ ] T022 [P] [US2] Create `src/services/aso/sse.ts` — `initSse(res)`, `sendSseEvent(res, event, data)`, `endSse(res)`
- [ ] T023 [US2] Create `src/services/aso/planGenerator.ts` — `buildPlanPrompt()`, `loadVisionImages(assetIds)`, `buildVisionMessages()` loading skill from `data/skills/aso_plan_generation.md`
- [ ] T024 [US2] Implement `parsePlansFromText(text): AsoPlan[]` in `planGenerator.ts` — JSON array + XML fallback; ids `plan_{ts}_{i}`
- [ ] T024a [US2] Implement `resolvePlanModel(assetIds)` — image materials present → `asoVisionAi`, else `universalAi`; fallback to text-only if asoVisionAi unconfigured
- [ ] T025 [US2] Implement `streamPlansToSse(res, req, options)` using `u.Ai.Text(modelKey).stream()` with multimodal messages when images present — emit `plan_start`, `plan_delta`, `plan_done`, `all_done`; handle `req.on('close')`
- [ ] T026 [US2] Implement `generatePlansSync(options): AsoPlan[]` using `invoke()` for non-stream fallback
- [ ] T027 [US2] Create `src/routes/aso/generatePlansStream.ts` — POST SSE; validate `inputText.trim() || assetIds.length || hasTextMaterials`
- [ ] T028 [US2] Create `src/routes/aso/generatePlans.ts` — POST JSON fallback returning same `plans[]` shape
- [ ] T029 [US2] Integrate `u.task()` with `taskClass='ASO方案生成'` on success/failure in planGenerator
- [ ] T030 [US2] Update `lastPlanGeneration.status` in workspace on start/done/error
- [ ] T031 [US2] Create `src/routes/aso/getWorkspace.ts` — return workspace + material summaries
- [ ] T032 [US2] Create `src/routes/aso/saveWorkspace.ts` — accept `patch` for inputText, planCount, selectedPlanId
- [ ] T033 [P] [US2] Create **Toonflow-web** `src/api/aso.ts` — `getWorkspace`, `saveWorkspace`, `generatePlansStream`, `generatePlans`
- [ ] T034 [US2] Create **Toonflow-web** `composables/useAsoPlanStream.ts` — fetch SSE parser + fallback to `generatePlans`
- [ ] T035 [US2] Create **Toonflow-web** `InputPanel.vue` — textarea, planCount (1–10), generate button wired to stream composable
- [ ] T036 [US2] Manual verify SSE event sequence per contracts/aso-api.md §7

**Checkpoint**: Generate 3 plans with streaming UI; empty text+no images returns 400

---

## Phase 5: User Story 3 — 编辑创意文案 (P1)

**Goal**: 用户可编辑方案 title/copy，保存后出图使用最新文案

**Independent Test**: POST `/api/aso/updatePlan` → GET workspace shows `edited: true` and new copy

### Implementation

- [ ] T037 [US3] Create `src/routes/aso/updatePlan.ts` — body: projectId, planId, title?, copy?; set edited=true, updatedAt
- [ ] T038 [US3] Add `updatePlan()` to **Toonflow-web** `src/api/aso.ts`
- [ ] T039 [US3] Create **Toonflow-web** `PlanList.vue` — list plans, inline edit, save calls updatePlan, select sets selectedPlanId via saveWorkspace

**Checkpoint**: Edit plan copy persists after page refresh

---

## Phase 6: User Story 4 — 素材管理与自动引用 (P1)

**Goal**: 导入图片 → `aso_material`；工作区展示引用；出图默认带引用素材

**Independent Test**: POST uploadMaterial → listMaterials shows item → referencedAssetIds includes id

### Implementation

- [ ] T040 [US4] Create `src/routes/aso/uploadMaterial.ts` — base64 → OSS `/{projectId}/aso/material/{uuid}.png` → o_assets(materialKind=image) + o_image; auto-append referencedAssetIds
- [ ] T040a [US4] Create `src/routes/aso/createTextMaterial.ts` — body: name, describe → o_assets(materialKind=text, no o_image); auto-append referencedAssetIds
- [ ] T041 [US4] Create `src/routes/aso/listMaterials.ts` — join o_image (optional), return materialKind + thumbnail URLs
- [ ] T042 [US4] Create `src/routes/aso/deleteMaterial.ts` — delete OSS+DB; remove from referencedAssetIds
- [ ] T043 [P] [US4] Add uploadMaterial, createTextMaterial, listMaterials, deleteMaterial to **Toonflow-web** `src/api/aso.ts`
- [ ] T044 [US4] Create **Toonflow-web** `MaterialGrid.vue` — image thumbnails + text material cards, checkbox for referencedAssetIds, delete, upload +「添加文字素材」
- [ ] T045 [US4] Wire InputPanel upload to uploadMaterial API

**Checkpoint**: Upload 2 images; both appear in grid and referencedAssetIds; delete removes from workspace

---

## Phase 7: User Story 5 — 生成 ASO 图 (P1)

**Goal**: 选定方案+素材 → 异步出图 → 轮询完成 → 历史保留

**Independent Test**: POST generateAsoImage → pollingOutputs until state=已完成 → file at correct OSS path

### Implementation

- [ ] T046 [P] [US5] Create `src/services/aso/imageGenerator.ts` — `resolvePreset()`, `loadAssetReferences()` (images only), `loadTextMaterialPrompts()`, `buildImagePrompt()`
- [ ] T047 [US5] Implement `runGenerateJob()` in imageGenerator.ts — Ai.Image.run (referenceList optional) → temp file → `resizeImage()` → OSS → update o_image + workspace output
- [ ] T047a [US5] Implement `isPlanOutputInProgress(projectId, planId)` in workspace.ts — check outputs[] or o_image state='生成中'
- [ ] T048 [US5] Create `src/routes/aso/generateAsoImage.ts` — if in-progress for planId return **409**; else insert aso_output + o_image(生成中); return ids; fire runGenerateJob async
- [ ] T049 [US5] Create `src/routes/aso/pollingOutputs.ts` — filter imageIds where state !== '生成中'
- [ ] T050 [US5] Integrate `u.task()` with `taskClass='ASO图生成'` in imageGenerator
- [ ] T051 [P] [US5] Add generateAsoImage, pollingOutputs to **Toonflow-web** `src/api/aso.ts`
- [ ] T052 [US5] Add「生成 ASO 图」button in PlanList/MaterialGrid area; **disable when planId in-progress**; poll until complete; show errorReason on failure
- [ ] T053 [US5] Create **Toonflow-web** `OutputGallery.vue` — list outputs[] from workspace + polling for in-progress

**Checkpoint**: Full path plan → image with or without materials; 409 on duplicate submit; retry on failure works

---

## Phase 8: User Story 6 — 选择 ASO 出图尺寸 (P1)

**Goal**: 12 项预设可选；默认 1080×1920；出图像素匹配 preset

**Independent Test**: Select `ios_screenshot_65` → output 1242×2688

### Implementation

- [ ] T054 [US6] Ensure generateAsoImage accepts `presetId` defaulting to workspace.outputSizePreset
- [ ] T055 [US6] Store presetId/width/height in workspace.outputs[] and o_image.resolution
- [ ] T056 [US6] Create **Toonflow-web** `SizePresetSelect.vue` — grouped iOS/Android/通用 dropdown
- [ ] T057 [US6] Wire SizePresetSelect to saveWorkspace `outputSizePreset` and generateAsoImage `presetId`
- [ ] T058 [US6] Manual verify default 1080×1920 and ios_screenshot_65 1242×2688 output dimensions

**Checkpoint**: Size selector changes output dimensions correctly

---

## Phase 9: User Story 7 — 参考图变体生成 (P2)

**Goal**: 文案+参考图 → 生成 N 张新 aso_material

**Independent Test**: POST generateRefVariants count=4 → 4 new materials in listMaterials

### Implementation

- [ ] T059 [P2] [US7] Implement `generateVariants()` in `src/services/aso/imageGenerator.ts`
- [ ] T060 [P2] [US7] Create `src/routes/aso/generateRefVariants.ts` — sourceAssetId, copy, count (1–10)
- [ ] T061 [P2] [US7] Integrate `u.task()` taskClass='ASO参考图变体'
- [ ] T062 [P2] [US7] Add variant panel in **Toonflow-web** MaterialGrid — count input + generate button

**Checkpoint**: P2 feature independently testable; MVP unaffected if skipped

---

## Phase 10: Polish & Cross-Cutting（联调发布）

**Purpose**: 整合 UI、回归、发布

- [ ] T063 [P] Assemble **Toonflow-web** `AsoWorkbench.vue` final layout: InputPanel | PlanList | MaterialGrid + SizePresetSelect | OutputGallery
- [ ] T064 Verify lazy workspace init on first getWorkspace (US1 project without prior ASO data)
- [ ] T065 Verify `delProject` cascades aso_material/aso_output (document in quickstart — no code change expected)
- [ ] T066 [P] Build **Toonflow-web** dist and copy to Toonflow-app `data/web/`
- [x] T067 Regression: create `novel` project — no ASO routes/UI visible
- [x] T068 Regression: create `script` project — script flow unchanged
- [x] T069 Regression: delete ASO project — OSS `/{id}/aso/` removed
- [x] T070 E2E manual: SC-001 five-minute flow (create → 3 plans → edit → image)
- [x] T071 Run `yarn dev:gui` Electron smoke test on ASO workbench
- [ ] T072 [P] Update fork README/CHANGELOG with ASO feature summary

**Checkpoint**: MVP shippable on branch `001-aso-creation`

---

## Dependencies & Execution Order

### Phase Dependencies

```text
Phase 1 (Setup)
    ↓
Phase 2 (Foundational) ← BLOCKS all user stories
    ↓
Phase 3 (US1) ─────────────────────────────┐
Phase 4 (US2) ← needs US1 routing stub     │
Phase 5 (US3) ← needs US2 plans            ├── sequential recommended
Phase 6 (US4) ← can parallel with US2 end  │
Phase 7 (US5) ← needs US3 + US4            │
Phase 8 (US6) ← needs US5                  │
Phase 9 (US7) ← P2 optional                │
    ↓                                      │
Phase 10 (Polish) ← after US1–US8 MVP ─────┘
```

### User Story Dependencies

| Story | Depends on | Can start after |
|-------|------------|-----------------|
| US1 | Phase 2 | T015 |
| US2 | Phase 2, US1 stub (T021) | T021 |
| US3 | US2 plans exist | T036 |
| US4 | Phase 2 | T015 (parallel with US2 backend) |
| US5 | US3, US4 | T045, T039 |
| US6 | US5 | T053 |
| US7 | US4 | T045 (P2 only) |

### Within-Story Order

1. Backend service → route → manual curl
2. **Toonflow-web** api.ts method → component → wire in AsoWorkbench
3. Checkpoint before next story

---

## Parallel Opportunities

### Phase 1 (all parallel)

```text
T001 projectTypes.ts  ||  T002 asoSizePresets.ts  ||  T003 types.ts  ||  T004 skill.md
```

### Phase 2 + early US4 backend

```text
After T015:
  Developer A: T022–T036 (US2 SSE)
  Developer B: T040–T045 (US4 materials)
```

### Frontend after API stable

```text
T033 api/aso.ts  then parallel:
  T035 InputPanel  ||  T039 PlanList  ||  T044 MaterialGrid  ||  T056 SizePresetSelect
```

---

## Implementation Strategy

### MVP First（US1–US6, skip US7）

1. Complete Phase 1 + Phase 2
2. US1 → US2 → US3 → US4 → US5 → US6（each checkpoint）
3. Phase 10 polish
4. **STOP**: SC-001 validated

### Incremental Delivery

| Increment | Delivers | Tasks |
|-----------|----------|-------|
| Inc 1 | ASO project + empty workbench | T001–T021 |
| Inc 2 | Stream plans | T022–T036 |
| Inc 3 | Edit + materials | T037–T045 |
| Inc 4 | Generate image | T046–T053 |
| Inc 5 | Size presets | T054–T058 |
| Inc 6 | Ship | T063–T072 |

### Single Developer Timeline

| Week | Tasks |
|------|-------|
| W1 | T001–T015, T016–T036 |
| W2 | T037–T058, T063–T072 |
| W3 (opt) | T059–T062 (P2) |

---

## Task Summary

| Phase | Tasks | Story | MVP |
|-------|-------|-------|-----|
| 0 Repo Setup | T000–T000b | — | 3 |
| 1 Setup | T001–T005, T004a | — | 6 |
| 2 Foundational | T006–T015 | — | 10 |
| 3 US1 | T016–T021 | 创建项目 | 6 |
| 4 US2 | T022–T036, T024a | 流式方案+Vision | 16 |
| 5 US3 | T037–T039 | 编辑文案 | 3 |
| 6 US4 | T040–T045, T040a | 素材(图/文) | 7 |
| 7 US5 | T046–T053, T047a | 出图+409 | 9 |
| 8 US6 | T054–T058 | 尺寸 | 5 |
| 9 US7 | T059–T062 | 变体 P2 | 0 |
| 10 Polish | T063–T072 | — | 10 |
| **Total** | **79** | | **75 MVP** |

---

## Definition of Done

- [ ] Task code on branch `001-aso-creation`
- [ ] `yarn lint` passes (Toonflow-app)
- [ ] Independent Test for user story passes (manual)
- [ ] novel/script regression unaffected
- [ ] Matches [contracts/aso-api.md](./contracts/aso-api.md)

---

## Notes

- 后端路径：`E:\workflow\toonflow\`（Toonflow-app fork）
- 前端路径：`E:\workflow\toonflow-web\`（Toonflow-web fork，Phase 0 T000）
- Vision 方案生成：`asoVisionAi` 部署槽 + 多模态 messages（C-005）
- 出图防重复：409 + UI disabled（C-007）
- 素材：图片或文字描述；出图不强制素材（C-008）
- 下一步：`/speckit.implement` 从 **T001** 开始
